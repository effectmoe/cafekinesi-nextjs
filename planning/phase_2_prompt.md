@planning/claude_execution_plan.md を読み込んでください。
Phase 1 が完了しました。続いて **Phase 2: ビルドと検証** を開始します。

**状況:**
- React 19非対応のライブラリ (`react-simple-maps`, `react-twitter-embed`) があります。
- `next-sanity` が Next.js 16 に対して Peer Dependency Warning を出しています。

**具体的な指示:**

1.  **依存関係の強制インストール**:
    - Peer Dependency エラーを回避するため、まずは以下のコマンドを実行してください。
    - `npm install --legacy-peer-deps`
    - インストールが成功するか確認してください。

2.  **Lint / Type Check の実行**:
    - `npm run lint` を実行し、主要なエラーがないか確認してください。
    - `npx tsc --noEmit` を実行し、型エラーが大量に出ていないか確認してください。
    - 軽微なエラーであれば修正してください。あまりに多い場合は、一旦警告として記録し、ビルドに進んでください。

3.  **ビルドテスト**:
    - `npm run build` を実行してください。
    - ビルドが失敗した場合、その原因が上記ライブラリに起因するものか分析してください。
    - 必要であれば、エラーの原因となっているコンポーネントの使用箇所を特定し、一時的にコメントアウトする等の対処案を提示してください。

4.  **報告**:
    - ビルドの成否と、失敗した場合はそのログの要約を報告してください。
